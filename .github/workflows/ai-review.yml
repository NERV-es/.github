name: AI Review

# Thin caller. Logic lives in NERV-es/.github (SHA-pinned).
# Free GitHub Models code review on every PR. Advisory only.

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write
  models: read

jobs:
  review:
    uses: NERV-es/.github/.github/workflows/ai-review-reusable.yml@0d85cf94394682b3625a8b18d6fe05aa085ab2fd
    secrets: inherit