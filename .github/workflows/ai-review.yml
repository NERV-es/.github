name: AI Review

# Thin caller. Logic lives in NERV-es/.github (SHA-pinned).
# Free GitHub Models code review on every PR. Advisory only.

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write
  models: read

jobs:
  review:
    uses: NERV-es/.github/.github/workflows/ai-review-reusable.yml@62b60f00abd929d15fb73801d8c30e9990a0ff9a
    secrets: inherit