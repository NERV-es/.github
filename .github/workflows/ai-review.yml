name: AI Review

# Thin caller. Logic lives in NERV-es/.github (SHA-pinned).
# Free GitHub Models code review on every PR. Advisory only.

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write
  models: read

jobs:
  review:
    uses: NERV-es/.github/.github/workflows/ai-review-reusable.yml@a36b2bde6f96529c935cb2bd7ae0d7772bbed4ea
    secrets: inherit